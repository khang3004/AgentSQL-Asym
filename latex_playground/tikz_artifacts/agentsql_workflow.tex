\documentclass[tikz, border=16pt]{standalone}
\usepackage{tikz}
\usepackage{amsmath}
\usetikzlibrary{shapes.geometric, arrows.meta, positioning, shadows.blur, backgrounds, fit, calc}

\begin{document}

\begin{tikzpicture}[
        node distance=1.2cm and 2.5cm,
        font=\sffamily\footnotesize,
        >=Stealth,
        % --- Styles ---
        phase/.style={
                rectangle, minimum width=5.2cm, minimum height=1.1cm,
                text centered, rounded corners=4pt, line width=0.7pt,
                blur shadow={shadow blur steps=6, shadow xshift=0.5pt, shadow yshift=-1pt}
            },
        io/.style={
                trapezium, trapezium left angle=70, trapezium right angle=110,
                minimum width=3.5cm, minimum height=0.9cm,
                text centered, draw=io_border, fill=io_fill,
                line width=0.8pt, blur shadow={shadow blur steps=4}
            },
        decision/.style={
                diamond, aspect=2.5, minimum width=2.8cm,
                inner sep=1pt, text centered, draw=sandbox_border, fill=sandbox_fill,
                line width=0.8pt, blur shadow={shadow blur steps=4}
            },
        arrow/.style={thick, ->, line width=0.8pt, draw=black!55},
        feedback/.style={thick, ->, dashed, line width=0.8pt, draw=red!55},
        label/.style={font=\sffamily\scriptsize, text=black!60}
    ]

    % ── Colour Palette ──────────────────────────────────────────────────
    \definecolor{chess_fill}{RGB}{227, 242, 253}
    \definecolor{chess_border}{RGB}{33, 150, 243}
    \definecolor{mci_fill}{RGB}{243, 229, 245}
    \definecolor{mci_border}{RGB}{156, 39, 176}
    \definecolor{asm_fill}{RGB}{232, 245, 233}
    \definecolor{asm_border}{RGB}{76, 175, 80}
    \definecolor{gen_fill}{RGB}{232, 245, 233}
    \definecolor{gen_border}{RGB}{76, 175, 80}
    \definecolor{reflect_fill}{RGB}{255, 253, 231}
    \definecolor{reflect_border}{RGB}{255, 179, 0}
    \definecolor{critic_fill}{RGB}{255, 235, 238}
    \definecolor{critic_border}{RGB}{229, 57, 53}
    \definecolor{sandbox_fill}{RGB}{236, 239, 241}
    \definecolor{sandbox_border}{RGB}{96, 125, 139}
    \definecolor{io_fill}{RGB}{255, 248, 225}
    \definecolor{io_border}{RGB}{255, 160, 0}

    % ── ANCHOR NODE for absolute positioning ────────────────────────────
    % We place all phase nodes manually via absolute y-coords to give
    % full control over vertical spacing for labels.

    % Input (row 0)
    \node (input) [io] {\textbf{NL Question} $Q$};

    % Phase 1 (row 1) — 1.4cm below input
    \node (chess) [phase, fill=chess_fill, draw=chess_border, below=1.4cm of input]
    {\textbf{Phase 1: CHESS Pruning} \\[4pt] \scriptsize FAISS Top-$k$ retrieval};

    % Phase 2 (row 2)
    \node (mci) [phase, fill=mci_fill, draw=mci_border, below=0.9cm of chess]
    {\textbf{Phase 2: MCI-SQL Enrichment} \\[4pt] \scriptsize MIN/MAX, cardinality, samples};

    % Phase 3 (row 3)
    \node (asm) [phase, fill=asm_fill, draw=asm_border, below=0.9cm of mci]
    {\textbf{Phase 3: Context Assembly} \\[4pt] \scriptsize DDL + MCI JSON + MAGIC guidelines};

    % ── GAP between offline box and LLM loop box ──────────────────────
    % 2.6cm vertical gap: enough room for (a) offline_box bottom border,
    % (b) the "ASYMMETRIC LLM LOOP" label sitting above llm_box's top border,
    % (c) the P_enriched arrow label, and (d) llm_box top border itself.
    \node (gen) [phase, fill=gen_fill, draw=gen_border, below=2.6cm of asm]
    {\textbf{Phase 4a: Generator} \\[4pt] \scriptsize \texttt{openai/gpt-oss-120b} via Groq};

    % Phase 4ab
    \node (reflect) [phase, fill=reflect_fill, draw=reflect_border, below=0.9cm of gen]
    {\textbf{Phase 4ab: Reflector} \\[4pt] \scriptsize \texttt{meta-llama/llama-4-scout-17b}};

    % Semantic Validation diamond
    \node (sandbox) [decision, below=0.9cm of reflect]
    {\scriptsize Semantic \\[2pt] \scriptsize Validation};

    % Output — below sandbox
    \node (output) [io, below=1.1cm of sandbox] {\textbf{Final SQL} $\hat{y}$};

    % Critic — to the right of sandbox
    \node (critic) [phase, fill=critic_fill, draw=critic_border,
        right=3cm of sandbox]
    {\textbf{Phase 4c: Resilient Critic} \\[4pt] \scriptsize \texttt{openai/gpt-oss-20b}};

    % ── ARROWS ──────────────────────────────────────────────────────────

    \draw [arrow] (input)   -- node[label, right] {} (chess);
    \draw [arrow] (chess)   -- node[label, right] {$\mathcal{S}' \subseteq \mathcal{S}$} (mci);
    \draw [arrow] (mci)     -- node[label, right] {$\mathbf{C}_{\text{meta}}$} (asm);
    % P_enriched label — this arrow crosses the inter-box gap, label goes on right side
    \draw [arrow] (asm)     -- node[label, right] {$P_{\text{enriched}}$} (gen);
    \draw [arrow] (gen)     -- node[label, right] {$y_0$} (reflect);
    \draw [arrow] (reflect) -- node[label, right] {\textcolor{gen_border}{\textsc{ok}}} (sandbox);

    % PASS arrow: offset LEFT (-7pt) so it is clearly separated from the feedback re-entry arrow
    \draw [arrow]
    ([xshift=-7pt]sandbox.south) --
    node[label, left] {\textsc{pass}}
    ([xshift=-7pt]output.north);

    % Feedback: sandbox error → critic
    \draw [feedback] (sandbox.east) -- node[label, above] {\scriptsize Syntax/Empty/Null err} (critic.west);

    % Feedback: reflector logic mismatch → critic
    \draw [feedback] (reflect.east) -| node[label, above, near start] {\scriptsize Logic mismatch} (critic.north);

    % Feedback: critic → sandbox, offset RIGHT (+7pt) on sandbox south to avoid PASS overlap
    \draw [feedback] (critic.south) |- ++(0,-0.8) -|
    node[label, below, near start] {\scriptsize $y_{i+1}$}
    ([xshift=7pt]sandbox.south);

    % ── BACKGROUND GROUPS ───────────────────────────────────────────────
    \begin{scope}[on background layer]

        % ── Offline pre-processing box ──────────────────────────────────
        \node[draw=chess_border!60, dashed, line width=0.8pt,
            fill=chess_fill!15, inner sep=10pt,
            rounded corners=8pt,
            fit=(chess)(mci)(asm)] (offline_box) {};

        % Side tab: the caption follows the dashed outline and saves horizontal space.
        \node[anchor=center,
            rotate=90,
            font=\sffamily\bfseries\scriptsize,
            text=chess_border!80,
            fill=white, inner xsep=3pt, inner ysep=1pt]
        at (offline_box.west)
        {OFFLINE PRE-PROCESSING (zero API cost)};

        % ── Asymmetric LLM loop box ─────────────────────────────────────
        \node[draw=critic_border!40, dashed, line width=0.8pt,
            fill=critic_fill!10, inner sep=10pt,
            rounded corners=8pt,
            fit=(gen)(reflect)(sandbox)(critic)] (llm_box) {};

        % Same side-tab pattern to save space while avoiding the center arrow.
        \node[anchor=center,
            rotate=90,
            font=\sffamily\bfseries\scriptsize,
            text=critic_border!70,
            fill=white, inner xsep=3pt, inner ysep=1pt]
        at (llm_box.west)
        {ASYMMETRIC LLM LOOP ($\leq 3$ API calls)};

    \end{scope}

\end{tikzpicture}
\end{document}
